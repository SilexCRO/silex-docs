# Experimental API quickstart

This command is illustrative only. It will not work until an official public Silex API URL and route are confirmed and published.

Replace `https://api.example.silex.invalid` with the approved public API base URL only after publication.

```bash
curl --fail -H "Accept: application/json" "https://api.example.silex.invalid/v1/public/projects/example-project"
```

The placeholder domain is intentionally non-routable and contains no production identifier.

