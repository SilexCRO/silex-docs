# DEX liquidity concepts

Liquidity integrations can expose public pair, pool, network, and timestamp information when the source and license permit it. Treat values as timestamped observations, not guarantees, and do not disclose private collection logic.

