# RPC usage

Use an RPC endpoint supplied by your own infrastructure or an approved public provider. Keep endpoints in local configuration and use `https://rpc.example.invalid` in examples. Silex documentation does not publish private RPC endpoints or infrastructure identifiers.

