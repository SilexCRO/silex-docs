# Developer tools

Build integrations around documented public interfaces and small, explicit use cases. Keep Silex calls behind your own adapter so API versions and error handling can evolve safely.

